class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        num = len(List)
        i = 0
        j = 0
        while i <= num:
            while j <= num & j >i:
                if List[i] + List[j] == target
                    break
            List.append(i)
            List.append(j)

